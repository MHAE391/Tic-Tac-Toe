<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Change the color as desired -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Change the color as desired -->
    <corners android:radius="0dp" /> <!-- Adjust the corner radius -->
</shape>
